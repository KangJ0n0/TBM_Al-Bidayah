---
import Layout from '../../layouts/Layout.astro';
import { documents } from '../../data/documents.js';
---

<Layout title="Daftar Dokumen TBM Al-Bidayah">
  <main class="py-16 md:py-24 bg-gray-50 text-gray-800">
    <div class="container mx-auto px-4 md:px-6">
      <h1 class="text-4xl md:text-5xl font-bold font-serif text-center mb-6 text-red-700">Dokumen</h1>
      <p class="text-xl text-gray-700 text-center max-w-3xl mx-auto mb-12">
        Kumpulan dokumen penting TBM Al-Bidayah yang dapat Anda baca atau unduh.
      </p>

      <div class="bg-white shadow-lg rounded-lg overflow-hidden">
        <table class="w-full border-collapse">
          <thead>
            <tr class="bg-red-700 text-white">
              <th class="py-4 px-6 text-left">Judul Dokumen</th>
              <th class="py-4 px-6 text-center w-40">Aksi</th>
            </tr>
          </thead>
          <tbody>
            {documents.map((doc, index) => (
              <tr class={`border-b ${index % 2 === 0 ? 'bg-gray-50' : 'bg-white'} hover:bg-gray-100 transition`}>
                <td class="py-4 px-6">{doc.title}</td>
                <td class="py-4 px-6 text-center">
                  <a href={`/dokumen/${doc.slug}`} class="text-red-700 hover:text-red-500 font-medium">Buka</a>
                  <span class="mx-2 text-gray-400">|</span>
                  <a href={doc.file} download class="text-red-700 hover:text-red-500 font-medium">Unduh</a>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    </div>
  </main>
</Layout>
